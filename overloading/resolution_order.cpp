
// source
// more exception item 10 P66
// (on function overloading) non-template function is preferred only
// if it is an exact match
// e.g. f(int, float) is NOT an exact match to f(int, double)

// this is also explained in depth in:
// Modern Effective C++
// C++ templates: the complete guide chapter 1

int main() {
    return 0;
}
